<h5>Drink wine</h5>

<%= form_for :bottles, :url => {action: 'drink_wine', controller: 'user_wines'}  do |f| %>

	    <div class="field">
	    	<%= f.label :quantity %>
	    	<%= f.number_field :quantity %>
	    </div>

	<%= token_tag %>

<% end %>